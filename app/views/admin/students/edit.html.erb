<%= simple_form_for @student, url: admin_student_path(@student), method: :patch do |f| %>
  <%= f.input :first_name %>

  <%= f.input :last_name %>

  <%= f.input :email %>

  <%= f.input :study_type, include_blank: false %>

  <%= f.button :submit %>

<% end %>