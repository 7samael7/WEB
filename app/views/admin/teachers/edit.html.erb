<%= simple_form_for @teacher, url: admin_teacher_path(@teacher), method: :patch do |f| %>
  <%= f.input :first_name %>

  <%= f.input :last_name %>

  <%= f.input :email %>

  <%= f.button :submit %>
<% end %>