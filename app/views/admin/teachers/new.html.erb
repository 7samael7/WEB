<%= simple_form_for @teacher, url: admin_teachers_path(@teacher), method: :post do |f| %>
  <%= f.input :first_name %>

  <%= f.input :last_name %>

  <%= f.input :email %>

  <%= f.button :submit %>
<% end %>