<%= form_for [@building, @room, @course], as: :faker do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :code %>
  <%= f.text_field :code %>

  <%= f.submit :save %>
<% end %>